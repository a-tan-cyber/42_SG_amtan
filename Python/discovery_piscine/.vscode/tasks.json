{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "echo",
      "type": "shell",
      "command": "echo Hello"
    },
    {
      "label": "chmod+run | cat -e",
      "type": "shell",
      "options": {
        "cwd": "${fileDirname}",
        "shell": { "executable": "/bin/bash", "args": ["-lc"] } // use bash; remove/adjust on mac if you prefer zsh
      },
      "command": "chmod +x \"${fileBasename}\" && ./\"${fileBasename}\" | cat -e",
      "problemMatcher": [],
      "presentation": { "reveal": "always", "panel": "dedicated" }
    }
  ]
}
